version: 2

sources:
  - name: stripe
    database: raw
    tables:
      - name: payment
        description: Raw payment data.
        columns:
          - name: id
            description: Primary key for payments